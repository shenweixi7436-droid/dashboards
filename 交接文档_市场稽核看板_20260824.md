# 市场稽核看板交接文档（2026-08-24）

## 1. 项目定位

本项目为本地离线 HTML 看板。Excel 是数据源，Python 脚本将数据写入 `assets/data/*.js`，页面文件负责筛选、图表和弹窗交互。

| 用途 | 路径 |
| --- | --- |
| 实际项目（当前维护入口） | `C:\Users\shenw\Documents\New project\github-dashboards\` |
| 维护源副本 | `C:\Users\shenw\Documents\Codex\2026-07-29\referenced-chatgpt-conversation-this-is-untrusted\full-project\github-dashboards\` |
| 主看板 | `index.html` |
| 年度陈列稽核总看板 | `display-audit-dashboard.html` |
| 主生成脚本 | `generate_v2.py` |
| 重点工作数据脚本 | `update_work_month_data.py` |
| 一键更新入口 | `一键更新全部看板数据.bat` |

Excel 数据源：

- `C:\Users\shenw\Desktop\看板\稽核与计划明细-2026年年度.xlsx`
- `C:\Users\shenw\Desktop\看板\市场稽核部重点工作.xlsx`

## 2. 当前基线

- 当前项目 Git 工作区在交接时为干净状态；最近提交为 `8a25e18`（2026-08-21，更新821）。
- `generate_v2.py` 与 `update_work_month_data.py` 已和维护源副本一致。
- `index.html`、`display-audit-dashboard.html` 与维护源副本目前不一致：后续如在维护源副本修改页面，必须先合并当前项目版本，不能直接回拷，以免覆盖 8 月中下旬的页面更新。

## 3. 已完成的重要修改

### 3.1 智能设备模块（`index.html`）

- 智能设备与月份选择已解耦，固定读取 `MAIN_DEFAULT_MONTH` 的设备快照。
- 标准设备开机率按 `开机数量 / 已投放` 计算；海尔冰柜按 `开机数 / 系统数据` 计算。
- 顶部智能设备卡展示四类设备整体开机率，点击仍打开智能设备弹窗。
- 智能设备弹窗：
  - “各设备开机数及开机率”改为“各设备开机数及投放数”。
  - 保温柜、烤肠机、星星冰柜右侧展示投放数，而非开机率。
  - 海尔渠道中的“台账数据 / 系统数据”改为“台账出库数据 / 系统出库数据”。

### 3.2 主看板陈列稽核弹窗（`index.html`）

- 弹窗底部“各省区稽核进度”与“稽核量与合格率”已改为等宽。
- 左侧省区进度已取消内部滚动，当前全部省区直接展示；右侧气泡图随容器等高。
- 气泡图横轴按跨度自动收疏刻度，避免标签过密。
- 修复打开业务弹窗后仍可点击底层“陈列稽核”整卡跳转的问题：透明链接层级已降低，并且弹窗显示期间会阻断跳转。

### 3.3 2026 年度全国陈列稽核总看板（`display-audit-dashboard.html`）

- 恢复“全年”月份入口。`assets/data/dashboard-snapshot.js` 中仍保留 `全年` 数据，全国和各战区均可使用。
- `generate_v2.py` 已修复：仅年度陈列稽核总看板在月份栏写入“全年”，主看板仍仅显示 1—12 月。以后执行一键更新不会删除全年入口。
- 删除各弹窗顶部“当前筛选 / 全部·月份”展示卡；删除的是显示内容，不是筛选状态。
- 月份、全年和战区筛选仍会驱动弹窗数据；年度趋势中的“当前月份”指标保留。

## 4. 数据更新流程

运行 `一键更新全部看板数据.bat`。执行顺序如下：

1. `generate_v2.py`：更新主看板、年度陈列稽核总看板、`dashboard-snapshot.js` 和当月稽核进度数据。
2. 更新推广稽核、结案审核、设备渠道与设备明细数据。
3. `update_work_month_data.py`：更新重点工作、智能设备等月度数据。
4. 更新鸣忙专项、进度看板与物料看板。

更新后建议打开 `index.html` 与 `display-audit-dashboard.html`，使用 `Ctrl+F5` 强制刷新。

## 5. 修改红线与验证清单

- 不改变既有指标定义、去重规则或 Excel 字段口径，除非用户明确确认。
- 不整体重写 `index.html` 或 `display-audit-dashboard.html`；保留字体、图片、卡片布局、弹窗和数据文件。
- 任何页面改动后至少验证：
  - 月份、全年（年度陈列看板）和战区筛选。
  - 相关卡片打开弹窗后的内容。
  - 弹窗显示期间，底层页面链接不可点击。
  - 一键更新后页面入口与缓存版本是否仍正确。
- 如需同步维护源副本，优先从当前实际项目向维护源副本同步；同步前先比较哈希和差异。

## 6. 待处理事项

当前没有已确认的功能缺陷。维护源副本的两个页面文件与实际项目不一致是唯一需要注意的维护风险；后续需要使用维护源副本时，应先进行差异合并并重新验证。
