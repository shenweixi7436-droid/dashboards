# 物料看板交接文档（2026-09-08）

> 时间跨度：2026-08-31 ~ 2026-09-08
> 维护位置：`C:\Users\shenw\Desktop\看板\物料小组看板\`
> 历史交接：上一份为 `交接文档_物料看板_20260821.md`
> CODEX 副本：`C:\Users\shenw\Documents\Codex\2026-09-08\WORKBUDDY_物料看板交接文档_20260908.md`

## 一、当前状态

本次工作在桌面工作版完成了一系列前端重构与数据修复，**未发布网站、未提交 Git、未推送 GitHub**。本地打开 `主看版.html` 即可查看效果，确认无误后按项目既定流程 commit + push。

## 二、本次改动总览

### 2.1 设备发货明细弹窗重构（端到端）

| 改动 | 文件 | 说明 |
|---|---|---|
| 读 N 列（设备大类） | `update_material_dashboard_sources.py` `build_device_outbound_detail` | `/` 和空值归为"未分类"；6 个真实分类（烤箱/保温柜/烤肠机双层/冰柜/烤肠机单层/" /"） |
| 读 B 列（供应商清洗）→ supplier | 同上 | 品牌列改用 supplier，brand 关键词提取作 fallback |
| 新增周/月双维度字段 | 同上 | `lastWeekQty`/`thisWeekQty`/`lastWeekAmount`/`thisWeekAmount`/`prevMonthQty`/`thisMonthQty`/`prevMonthAmount`/`thisMonthAmount` |
| 兼容旧字段 | 同上 | 保留 `monthQty`/`weekQty`/`qtyDelta`/`amountDelta` 字段，进销存看板不破坏 |
| 前端周/月切换 | `主看版.html` | 弹窗头部加 dim-toggle（周/月），点击切换 |
| 大类分组折叠 | `主看版.html` | 每个 category 一个 `<tbody class="cat-group">`，带 ▶/▼ 展开折叠 |
| 金额万元格式化 | `主看版.html` | `formatMoneyWan`/`formatMoneyDeltaWan`：`¥4.64万` 格式 |
| 周维度不显示月份 | `主看版.html` | `deviceMonthSpanText` 分流：周维度返回"本周 08.29~09.04"，月维度返回"X月" |

### 2.2 主看版月份默认值与跨看板联动

| 改动 | 文件 | 说明 |
|---|---|---|
| 默认当前时间月份 | `主看版.html` `updateMonth` | `var currentMonth = new Date().getMonth()+1`；不再用 `freight` 数据最新月份回退 |
| 运费看板链接联动 | `主看版.html` | `freightLink.href='运费看板.html?month='+label` |
| 进销存看板链接联动 | `主看版.html` | `inventoryLink.href='物料进销存看板.html?month='+label`（URL 参数已加，接收端待扩展） |
| `dashboardmonthchange` 事件 | `主看版.html` | 月份变化时 dispatch 事件，设备/物料弹窗如已打开则自动刷新 |

### 2.3 物料库存明细弹窗重构（端到端）

| 改动 | 文件 | 说明 |
|---|---|---|
| 列错位修复 | `update_dashboard_data.py` `build_material_inventory` | 修正 G列(上周出库数)误为 safetyStock、H列(安全库存)、K列(补货提前期)、Q/S/V 列的错位 |
| `detailByMonth` 结构生成 | 同上 | 每月 134 条物料，含 `lastWeekOut`/`thisWeekOut`/`lastMonthOut`/`thisMonthOut` + 库存/安全库存/补货期/紧急程度/预计断货/备注 |
| 月聚合数据修复 | 同上 | **关键修复**：原用 L 列（月次，0 行真实数据）→ 改用 J 列（年月，格式"年-月-日"）`split("-")[1]` 提取月份 |
| 6 列布局 | `主看版.html` `buildMaterialInventoryTable` | 物料名称/上周或上月出库数/本周或本月出库数/环比差异/当前库存/预计消化时间 |
| 周/月维度切换 | `主看版.html` | 弹窗头部加 dim-toggle（复用设备弹窗类名） |
| `materialModalDim` 状态 | `主看版.html` | 镜像 `deviceModalDim` 模式 |
| 物料编码="停发"过滤 | `主看版.html` | 行渲染前过滤停发记录，134 条减去停发后展示 |

### 2.4 物料弹窗 4 项 UI 优化（09-08）

| 改动 | 文件 | 说明 |
|---|---|---|
| 预计消化时间统一为天数 | `主看版.html` `materialRowValues` | 删掉"X周Y天"/"X月Y天"混合格式，统一为 `Math.round(days)+'天'`；保留 `>1年`（≥365 天）和 `充足`（无出库/日均=0） |
| 色块作行底色 | `主看版.html` CSS | 删除 `tr:nth-child(even)` 斑马纹，新增 `tr.urg-紧急/断货/预警/正常/其他` 5 种底色（10-16% 透明度） |
| 色块点击筛选 | `主看版.html` `renderMaterialModalBody` | 色块加 `data-urg-filter` + cursor:pointer + active outline 高亮；点击 toggle `data-urg-active` 切换行显隐 |
| 本周/本月出库数排序 | `主看版.html` `sortMaterialInventoryTable` | 表头第 3 列加 `sortable` + sort-indicator (▼/▲)；按 `data-this-out` 数值切 asc/desc |
| 本周显示具体日期 | `主看版.html` `getActiveMaterialDetail` + `materialMonthSpanText` | 从 `deviceWeeklyData.weekRange` 取 thisLabel，显示"本周 08.29~09.04" |
| 维度消化天数解释 | `主看版.html` `materialDimHintText` | 弹窗顶端黄色色条 hint：周维度≈短期预警，月维度≈长期预测 |

## 三、关键文件位置

```
桌面工作版：
  C:\Users\shenw\Desktop\看板\物料小组看板\
  ├── 主看版.html  (本次所有前端改动)
  ├── 物料进销存看板.html
  ├── 运费看板.html
  ├── 2026年设备发货明细-共享版.xlsm  (用户已补填 N 列设备大类)
  ├── 库存分析看板源数据_含宏.xlsm
  ├── device_weekly_outbound_data.v2.js  (8/9 月设备归类已正确)
  ├── material_inventory_data.v2.js  (9 月 134 条物料，monthList=[1..9])
  └── update_dashboard_data.py  (build_material_inventory 已重写)
      update_material_dashboard_sources.py  (build_device_outbound_detail 已扩展)

更新入口：
  C:\Users\shenw\Documents\New project\github-dashboards\一键更新物料看板.bat
```

## 四、数据结构变更

### 4.1 `deviceWeeklyData` 新增字段

```json
{
  "weekRange": {"thisStart":"2026-08-29","thisEnd":"2026-09-04","thisLabel":"08.29~09.04", "prevStart":..., "prevEnd":..., "prevLabel":...},
  "detailByMonth": {
    "9": {
      "month": 9,
      "groups": [
        {
          "key": "...",
          "title": "...",
          "rows": [
            {"name":"...", "supplier":"...", "category":"烤箱", "lastWeekQty":..., "thisWeekQty":..., "lastWeekAmount":..., "thisWeekAmount":..., "prevMonthQty":..., "thisMonthQty":..., "prevMonthAmount":..., "thisMonthAmount":..., "monthQty":..., "weekQty":..., "qtyDelta":..., "amountDelta":...}
          ],
          "total": {...}
        }
      ]
    }
  }
}
```

### 4.2 `materialInventoryData` 顶层字段

```json
{
  "items": [...134 条物料兼容旧版...],
  "monthList": [1,2,3,4,5,6,7,8,9],
  "latestMonth": 9,
  "detailByMonth": {
    "9": {
      "month": 9,
      "items": [
        {"name":"...", "code":"...", "currentStock":..., "defaultMoq":..., "lastWeekOut":..., "thisWeekOut":..., "lastMonthOut":..., "thisMonthOut":..., "safetyStock":..., "leadTime":..., "urgency":"预警", "expectedOutage":"...", "remark":"..."}
      ]
    }
  },
  "sourceFile": "...",
  "sourceSheet": "库存源数据",
  "updatedAt": "2026-09-08 ..."
}
```

## 五、关键修复点

### 5.1 月维度数据缺失——已根因修复

**原诊断（错误）**：出入库流水与库存源数据实体不匹配。

**根因（数据探查验证）**：出入库流水"月次"列（L 列）65405 行中 0 行有真实月份数字（54835 行 None + 10570 行占位符"计算月次"）。原 `month = _to_int(row[11])` 全部返回 None → 所有记录被 `if not month: continue` 跳过 → `monthly_out` 字典为空 → 月维度数据全 0。

**修复**：改用 J 列（年月，格式"年-月-日"字符串如"2026-1-9"）`split("-")[1]` 提取月份。

**修复效果**：`monthly_out` 从 0 条 → 674 条；monthList 从 `[1-12]` fallback → `[1-9]` 真实数据；latestMonth 从 12 → 9；9 月有 34 条物料有真实出库数据。

**实体匹配率实际 93%**（库存源数据 199 条 ∩ 出入库流水 187 种商品 = 185），名称完全对得上。

### 5.2 物料列错位——已修正

`build_material_inventory` 原代码 G列(row[6])被误映射为 `safetyStock`，实际 G列=上周出库数。已重写为：

| 列 | 实际字段 | 索引 |
|---|---|---|
| A | 物料名称 | 0 |
| B | 物料编码 | 1 |
| C | 当前库存 | 2 |
| E | 默认起发量 | 4 |
| F | 本周出库数 | 5 |
| G | 上周出库数 | 6（**原误为 safetyStock**） |
| H | 安全库存 | 7 |
| K | 补货提前期(天) | 10 |
| Q | 紧急程度 | 16 |
| S | 预计断货日期 | 18 |
| V | 其他备注 | 21 |

### 5.3 设备大类（N 列）——用户已补填

`2026年设备发货明细-共享版.xlsm` 关键字段汇总 sheet N 列原为空，用户已补填"设备大类"列。补填后 8/9 月设备全部正确归类，零"未分类"。

## 六、周/月维度消化天数逻辑

**公式结构一致**：`currentStock / 期间出库总数 × 期间天数 = currentStock / 期间日均出库率`

| 维度 | 输入数据 | 反映 | 适用 |
|---|---|---|---|
| 周 | `thisWeekOut`（本周 7 天总出库） | 短期波动 | 短期预警（出库持续则 X 天后断货） |
| 月 | `thisMonthOut`（本月 30 天总出库） | 长期平均 | 长期预测（按月均速 X 天后断货） |

**为什么数值不一致**：周/月输入数据反映不同时间窗口的真实出库量。如本周出库暴增但月累计很低，消化天数差异显著——这是数据真实反映，不是 bug。弹窗顶端黄色 hint 已加解释。

## 七、日常本地更新流程

```
1. 修改源 Excel
   ├── 2026年设备发货明细-共享版.xlsm  (N 列设备大类已补填)
   ├── 库存分析看板源数据_含宏.xlsm
   └── 物料运费分析.xlsx / 物料开发进度跟进表.xlsx
2. 确认桌面工作版位置未变
3. 点击 一键更新物料看板.bat
   ├── update_material_dashboard_sources.py
   │   ├── build_device_outbound_detail  (8/9 月已含完整 category/supplier/周月字段)
   │   └── 其他 ...
   └── update_dashboard_data.py
       ├── build_material_inventory  (134 条 × 9 月，J 列提取月份)
       └── 其他 ...
4. 打开 主看版.html 检查设备和物料两个弹窗
   ├── 设备：弹窗头部 dim-toggle 周/月切换、大类分组折叠、万元格式
   └── 物料：弹窗头部 dim-toggle 周/月切换、6 列布局、色块可点击筛选、本周日期、消化天数 hint
5. 用 Ctrl+F5 强制刷新
6. 确认无误后 commit + push GitHub
```

## 八、发布前检查清单

- [ ] 设备弹窗：周/月切换正常，万元格式"¥4.64万"显示
- [ ] 设备弹窗：周维度 month-span 显示"本周 08.29~09.04"（无月份前缀）
- [ ] 设备弹窗：设备大类分组折叠 ▶/▼ 正常
- [ ] 物料弹窗：dim-toggle 周/月切换正常
- [ ] 物料弹窗：周维度使用 `thisWeekOut`/`lastWeekOut`，月维度使用 `thisMonthOut`/`lastMonthOut`（非全"—"）
- [ ] 物料弹窗：4 种底色按 urgency 应用到行（紧急红/断货棕/预警黄/正常绿）
- [ ] 物料弹窗：色块点击可筛选对应 urgency 物料
- [ ] 物料弹窗：本周/本月出库数列点击可切 asc/desc
- [ ] 物料弹窗：month-span 周维度显示具体日期（如"本周 08.29~09.04"）
- [ ] 物料弹窗：顶端黄色 hint 显示"短期预警/长期预测"解释
- [ ] 主看版顶部月份选择器默认 9 月（当前时间月份）
- [ ] 运费看板链接 `?month=9月`、进销存看板链接 `?month=9月`
- [ ] 切换主看版月份，设备/物料弹窗如已打开则自动刷新
- [ ] 用 `Ctrl+F5` 强制刷新避免旧 JS 缓存

## 九、当前未完成事项

- 设备弹窗"本周"为设备周次（周五至周四），物料弹窗"本周"为自然周（08.29~09.04）——口径暂未统一，**未做修复**。如需对齐需扩展物料数据源加入设备周次列。
- 进销存看板 `?month=` URL 参数已加但接收端暂未读取（接收端月选择器改造不在本次范围）。
- 本次未执行完整 BAT 生成、未发布网站、未 commit/push。
- 弹窗预览 mock 文件（`弹窗预览_mock.html`）只覆盖设备弹窗，物料弹窗未做独立预览。
- 物料搜索 + 色块筛选组合时未做"全部重置"按钮（如已筛选紧急+搜索"袋"，需手动逐个清除）。
